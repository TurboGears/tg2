<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" 
                      "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:py="http://genshi.edgewall.org/"
      xmlns:xi="http://www.w3.org/2001/XInclude">

<xi:include href="../master.html" />

<head>
  <meta content="text/html; charset=UTF-8" http-equiv="content-type" py:replace="''"/>
  <title py:if="page=='edit'">Edit $${modelname}</title>
  <title py:if="page=='new'">New $${modelname}</title>
</head>

<body>
<span py:if="page=='new'">
    <h1>New $${modelname}</h1>
    $${form(action='create', submit_text = "Create")}
</span>

<span py:if="page=='edit'">
<h1>Editing $${modelname}</h1>
$${form(value=record, action='../update/%d'%record.${id}, submit_text = "Update")}</span>
<br/>

<div id="footbar" class="footbar" py:if="page=='edit'">
<a href="$${tg.url('../show/%s'%record.${id})}">Show</a> | <a href="$${tg.url('../list')}">Back</a>
</div>

</body>
</html>
